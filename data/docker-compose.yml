version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: Ben
      POSTGRES_PASSWORD: postgres88
      POSTGRES_DB: WebExAg
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
